import { Controller, Get, Req } from '@nestjs/common';

@Controller('ingredients')
export class IngredientsController {

    // @Get('all')
    // getAll(@Req() request: Request): void {
    //     // return this.ingredientsService.getAll;
    // }
}
